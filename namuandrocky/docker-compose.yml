services:
  namuandrocky-backend:
    image: ghcr.io/mariannefeng/namu-and-rocky-backend:latest
    container_name: namu-and-rocky-backend
    restart: unless-stopped
    expose:
      - "8080"
    env_file:
      - ./secrets/.env
    environment:
      - R2_BUCKET=namu-and-rocky
      # Aggressively limit Node.js heap size for 512MB server
      - NODE_OPTIONS=--max-old-space-size=96
    # Set strict memory limits for constrained server
    deploy:
      resources:
        limits:
          memory: 192M
        reservations:
          memory: 64M
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
